import { AfterContentInit, Injector } from '@angular/core';
import { FormGroup } from '@angular/forms';
import { InputBase } from '../../models/InputBase';
import { FileService } from '../../directives/upload-images/test';
export declare class DynamicInputComponent implements AfterContentInit {
    private api;
    private injector;
    input: InputBase<any>;
    form: FormGroup;
    submitted: Boolean;
    isClear: Boolean;
    captchaCorrect: Boolean;
    validatorForm: any;
    constructor(api: FileService, injector: Injector);
    initTinyMce(test: any): any;
    ngAfterContentInit(): void;
    readonly isValid: boolean;
    getError(validators: any): string;
    getAsyncErrors(validators: any): string;
    onKeyup(e: any, k: any): void;
}
