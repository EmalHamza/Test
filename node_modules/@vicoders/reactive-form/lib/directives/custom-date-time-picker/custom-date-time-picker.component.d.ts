import { OnInit, ElementRef, ChangeDetectorRef, OnChanges } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
export declare class CustomDateTimePickerComponent implements ControlValueAccessor, OnInit, OnChanges {
    private cdRef;
    private element;
    _value: Date;
    showIcon: Boolean;
    monthNavigator: Boolean;
    yearNavigator: Boolean;
    yearRange: string;
    showTime: Boolean;
    timeOnly: Boolean;
    dateFormat: string;
    isDisabled: Boolean;
    minDate: Date;
    maxDate: Date;
    isClear: Boolean;
    dates: Date[];
    rangeDates: Date[];
    en: any;
    tr: any;
    invalidDates: Array<Date>;
    private disabled;
    private onChange;
    private onTouched;
    constructor(cdRef: ChangeDetectorRef, element: ElementRef);
    writeValue(val: any): void;
    value: Date;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    setDisabledState(isDisabled: boolean): void;
    ngOnChanges(): void;
    ngOnInit(): void;
    change(value: any): void;
}
