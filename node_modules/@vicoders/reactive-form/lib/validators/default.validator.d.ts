import { AbstractControl, ValidationErrors, AsyncValidatorFn } from '@angular/forms';
declare class Validators {
    constructor();
    isEqualValidator(matchValue: any): (control: AbstractControl) => ValidationErrors;
    isDifferentValidator(matchValue: any): (control: AbstractControl) => ValidationErrors;
    isExistValidator(url: any, message: any): AsyncValidatorFn;
}
export declare const DefaultValidators: Validators;
export {};
